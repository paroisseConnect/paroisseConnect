# =============================================================================
# Paroisse Connect — Fichier d'environnement pour le serveur (Docker Compose)
# =============================================================================
# Copier ce fichier en .env sur le serveur : cp .env.example .env
# Puis adapter les valeurs (notamment les mots de passe).
# Le .env ne doit pas être commité (il est dans .gitignore).
# =============================================================================

# -----------------------------------------------------------------------------
# Base de données PostgreSQL (conteneur postgres + tous les services Spring)
# -----------------------------------------------------------------------------
POSTGRES_DB=churchnow
POSTGRES_USER=church
# En production : remplacer par un mot de passe fort
POSTGRES_PASSWORD=church

# -----------------------------------------------------------------------------
# Ports exposés sur l'hôte (éviter les conflits avec d'autres apps sur le serveur)
# Valeurs par défaut du projet ; modifier si un port est déjà utilisé.
# -----------------------------------------------------------------------------
# PostgreSQL (accès depuis l'hôte : localhost:POSTGRES_PORT)
POSTGRES_PORT=5433

# Eureka (Discovery) — dashboard : http://<serveur>:DISCOVERY_PORT
DISCOVERY_PORT=8761

# Config Server (Spring Cloud Config)
CONFIG_PORT=8888

# Gateway — API publique et Swagger : http://<serveur>:GATEWAY_PORT
GATEWAY_PORT=8088

# Microservices (accès direct depuis l'hôte si besoin)
USER_SERVICE_PORT=8081
PARISH_SERVICE_PORT=8082
ACTIVITY_SERVICE_PORT=8083
COMMUNICATION_SERVICE_PORT=8084
CONTENT_SERVICE_PORT=8085
WORSHIP_SERVICE_PORT=8086
API_DOCUMENTATION_SERVICE_PORT=8087

# -----------------------------------------------------------------------------
# JWT (user-service génère le token au login ; gateway valide et ajoute X-User-Id)
# Même valeur sur user-service et gateway. En production : secret fort, au moins 32 caractères.
# -----------------------------------------------------------------------------
# JWT_SECRET=paroisse-connect-jwt-secret-change-in-production

# -----------------------------------------------------------------------------
# Optionnel : schéma PostgreSQL utilisé par les services (par défaut paroisse_schema)
# Déjà défini dans les SPRING_DATASOURCE_URL du docker-compose ; modifier seulement
# si vous utilisez un schéma différent.
# -----------------------------------------------------------------------------
# POSTGRES_SCHEMA=paroisse_schema
